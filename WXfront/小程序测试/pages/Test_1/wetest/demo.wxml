<!--pages/wetest/demo.wxml-->

<view>{{uid}}{{userid}}{{username}}{{password}}</view>

<!-- <view wx:for="{{userInfo}}">
  <view>序号：{{item.uid}}</view>
  <view>用过id：{{item.userid}}</view>
  <view>姓名：{{item.username}}</view>
  <view>密码：{{item.password}}</view>
</view> -->

<view class="table">
  <view class="tr bg-w">
    
    <view class="th">id</view>
    
    <view class="th">状态</view>
  </view>

  <block wx:for="{{userInfo}}" wx:key="{{id}}">
    <view class="tr bg-g" wx:if="{{index %2 == 0}}">
        <view class="td">{{item.parking_id}}</view>
        <view class="td">{{item.parking_status}}</view>
        <!-- <view class="td">{{item.username}}</view>
        <view class="td">{{item.password}}</view> -->
    </view>
    <view class="tr" wx:else>
        <view class="td">{{item.parking_id}}</view>
        <view class="td">{{item.parking_status}}</view>
        <!-- <view class="td">{{item.username}}</view>
        <view class="td">{{item.password}}</view> -->
    </view>
  </block>
</view>

<button bindtap="getMessage"> 测试 </button>
